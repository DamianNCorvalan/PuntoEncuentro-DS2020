@model IEnumerable<PuntoEncuento.Predio>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Domicilio.Calle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImagenPredio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Domicilio.Calle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImagenPredio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPredio }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPredio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPredio })
        </td>
    </tr>
}

</table>
